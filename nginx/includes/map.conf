# Static UUID-to-path mapping as emergency fallback
# Only used if auth_request fails or for public videos

# map $uri $static_video_path {
#     default "";
#     include /etc/nginx/includes/video_static.map;
# }
