# Additional rate limiting for specific endpoints
# Included by default.conf but can be extended here

# Stricter limits for login/registration endpoints
limit_req_zone $binary_remote_addr zone=login:10m rate=1r/s;

# Geo-based rate limiting (if geo module available)
# map $geoip_country_code $limit_country {
#     default 1;
#     US 0;
#     GB 0;
# }
